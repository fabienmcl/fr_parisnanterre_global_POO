package Burger;

public class Factory {
   /*
    getInstance(){
        if (eInstance == null){eInstance = new Factory();}
        return eInstance;
    }

    Burger create(Menu Type){
        Burger b;
        if type == M_P
            { b = new Burger (New Poisson()); b.ajouter(new Bearnaise())}
            ekse { fbdbfns (new Boeuf())}
        return b;
    }


    */
}
